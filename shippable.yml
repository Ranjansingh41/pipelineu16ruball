language: ruby

ruby: 
  - 2.3.8

services:
  - postgres 

build:
 #pre_ci_boot:
   ## image_tag: v7.1.4
   # pull: true
    
  ci:
    - java --version
    - source /etc/profile.d/rvm.sh
    - rvm list
    - ruby --version 
